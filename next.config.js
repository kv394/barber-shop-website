/** @type {import('next').NextConfig} */
const nextConfig = {
  // This empty object is all that's needed for a standard setup.
  // We are removing the complex Turbopack configuration for now
  // to ensure the server starts without issues.
};

module.exports = nextConfig;
